# Real FastAPI (default): omit this or set to false. Vite proxies /api → http://localhost:8000 (see vite.config.js).
# VITE_USE_MOCK=false

# Offline / static JSON only (set to true):
# VITE_USE_MOCK=true

# Console [map-perf] timings on the map page (also on in npm run dev):
# VITE_PERF_LOG=true
