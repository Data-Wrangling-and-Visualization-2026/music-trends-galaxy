# Copy to backend/.env and adjust. Used by local scripts (sync_db.py) and uvicorn.

# Postgres from repo docker-compose.yml (host port 5433 -> container 5432)
DATABASE_URL=postgresql+psycopg2://app:app@localhost:5433/app

# Optional: same defaults as in app/config.py if you omit them
# STORAGE_DIR=D:/DWaV_project/music-trends-galaxy/storage
